<% if user_signed_in? && @user != current_user %>
    <div id="js-followForm" class="mt-3">
        <% if current_user.following?(@user) %>
            <%= render 'unfollow' %>
        <% else %>
            <%= render 'follow' %>
        <% end %>
    </div>
<% end %>


