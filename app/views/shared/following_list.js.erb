$('#following_list').append("<%= escape_javascript(render partial: 'users', collection: @following_users, as: "user") %>");
$("#view_more").replaceWith("<%= escape_javascript(
link_to_next_page(@following_users, 'さらに表示', remote: true, params: { type: :following_list }, class: 'btn-warning btn btn-lg', id: 'view_more')) %>");